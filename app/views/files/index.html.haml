%div
  %h1.text-center Image Uploader and Converter
#new-image
  %h2{ style: "text-align: center;" } New Image
  = new_image_form
%div
  %h3 Images
  %table#image-table
    %tr
      %th Name
      %th Progress
      %th Status
    - @images.each do |image|
      %tr{ id: "image-#{image.id}" }
        %td= link_to image.name, image_path(image)
        %td
          .progress.progress-striped
            .progress-text
              100%
            .bar{ style: "width: 100%;" }
        %td
          = image.status